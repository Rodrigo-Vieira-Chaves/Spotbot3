import { expect } from 'chai';
import { describe, it } from 'mocha';

describe('Teste1', () => {
  const teste = 'sadasdd';

  it('Should print hello in console1', () => {
    expect(teste).to.be.eq(teste);
  });
});
