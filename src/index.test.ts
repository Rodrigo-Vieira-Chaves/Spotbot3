import { expect } from 'chai';
import { describe, it } from 'mocha';

describe('Teste', () => {
  const teste = '';

  it('Should print hello in console', () => {
    expect(teste).to.be.eq(teste);
  });
});
